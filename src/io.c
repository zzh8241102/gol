#include <stdio.h>
#include "state.h"

// parse the .ini file and return a 2-d array comprise 0-1s
// Optional with same pre-writed model : grid
int file_parser(FILE* file, game_state *game){

}

// Advanced listen to the current grid_created by the click_operation

// write the final result to file
int write_result(FILE* file, game_state *game){

}